# RBAC Qualification Contract

Status: `PLANNED`

This file replaces the former executable placeholder `security/controls/rbac.py`. The old Python file did not implement usable behavior and therefore must not count as implementation or test evidence.

Required real implementation/qualification:

- VIEWER read-only behavior
- OPERATOR bounded operations
- ADMIN configuration boundary
- MIGRATION_OWNER migration-only boundary
- negative/SoD tests
- audit evidence
