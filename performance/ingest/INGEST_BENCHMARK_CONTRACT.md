# Ingest Benchmark Contract

Status: `PLANNED`

This file replaces the former executable placeholder `performance/ingest/ingest_benchmark.py`. The old Python file did not implement usable behavior and therefore must not count as implementation or test evidence.

Required real implementation/qualification:

- syslog events/sec
- parser throughput
- database write throughput
- queue pressure/drops
- CPU and memory impact
- 24h/72h sustained evidence
